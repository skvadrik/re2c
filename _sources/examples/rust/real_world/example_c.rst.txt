C lexer in Rust
---------------

This is an example of a C lexer written in Rust.
Compile as ``re2rust -o c.rs c.re --no-unsafe``.

:download:`[c.re] <c.re>`

.. literalinclude:: c.re
    :language: rust


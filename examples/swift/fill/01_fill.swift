/* Generated by re2swift */
#sourceLocation(file: "01_fill.re", line: 1)
// re2swift $INPUT -o $OUTPUT
import Foundation

struct Input {
  static let bufferSize = 4095

  var file: FileHandle

  var buffer   = ContiguousArray<UInt8>(repeating: 0, count: Self.bufferSize + 1)
  var yylimit  = Self.bufferSize
  var yycursor = Self.bufferSize
  var yymarker = Self.bufferSize
  var token    = -1
  var eof      = false
}

extension Input {
  mutating func lex() -> Int? {
    var count = 0
    loop: while true {
      self.token = self.yycursor
      
#sourceLocation(file: "01_fill.swift", line: 26)
  var yych: UInt8 = 0
  var yystate: UInt = 0
  while true {
    switch yystate {
      case 0:
        yych = self.buffer[self.yycursor]
        switch yych {
          case 0x20:
            self.yycursor += 1
            yystate = 3
            continue
          case 0x27:
            self.yycursor += 1
            yystate = 5
            continue
          default:
            if self.yylimit <= self.yycursor {
              if self.fill() == .ok {
                yystate = 0
                continue
              }
              yystate = 10
              continue
            }
            self.yycursor += 1
            yystate = 1
            continue
        }
      case 1:
        yystate = 2
        continue
      case 2:
#sourceLocation(file: "01_fill.re", line: 37)
        return nil
#sourceLocation(file: "01_fill.swift", line: 61)
      case 3:
        yych = self.buffer[self.yycursor]
        switch yych {
          case 0x20:
            self.yycursor += 1
            yystate = 3
            continue
          default:
            if self.yylimit <= self.yycursor {
              if self.fill() == .ok {
                yystate = 3
                continue
              }
            }
            yystate = 4
            continue
        }
      case 4:
#sourceLocation(file: "01_fill.re", line: 35)
        continue loop
#sourceLocation(file: "01_fill.swift", line: 82)
      case 5:
        self.yymarker = self.yycursor
        yych = self.buffer[self.yycursor]
        if yych >= 0x01 {
          yystate = 7
          continue
        }
        if self.yylimit <= self.yycursor {
          if self.fill() == .ok {
            yystate = 5
            continue
          }
          yystate = 2
          continue
        }
        self.yycursor += 1
        yystate = 6
        continue
      case 6:
        yych = self.buffer[self.yycursor]
        yystate = 7
        continue
      case 7:
        switch yych {
          case 0x27:
            self.yycursor += 1
            yystate = 8
            continue
          case 0x5C:
            self.yycursor += 1
            yystate = 9
            continue
          default:
            if self.yylimit <= self.yycursor {
              if self.fill() == .ok {
                yystate = 6
                continue
              }
              yystate = 11
              continue
            }
            self.yycursor += 1
            yystate = 6
            continue
        }
      case 8:
#sourceLocation(file: "01_fill.re", line: 31)
        
          count += 1
          continue loop

#sourceLocation(file: "01_fill.swift", line: 134)
      case 9:
        yych = self.buffer[self.yycursor]
        if yych <= 0x00 {
          if self.yylimit <= self.yycursor {
            if self.fill() == .ok {
              yystate = 9
              continue
            }
            yystate = 11
            continue
          }
          self.yycursor += 1
          yystate = 6
          continue
        }
        self.yycursor += 1
        yystate = 6
        continue
      case 10:
#sourceLocation(file: "01_fill.re", line: 36)
        return count
#sourceLocation(file: "01_fill.swift", line: 156)
      case 11:
        self.yycursor = self.yymarker
        yystate = 2
        continue
      default: fatalError("internal lexer error")
    }
  }
#sourceLocation(file: "01_fill.re", line: 38)

    }
  }

  mutating func fill() -> FillStatus {
    guard !self.eof else {
      return .eof
    }

    let shift = self.token
    let used = self.yylimit - self.token
    let free = Self.bufferSize - used

    // Error: Lexeme too long. In the real world we can reallocate a larger buffer.
    if self.token < 1 {
      return .longLexeme
    }

    // Shift buffer contents, discarding everything up to the current lexeme.
    self.buffer.replaceSubrange(..<used, with: self.buffer[shift..<self.yylimit])
    self.yylimit  -= shift
    self.yycursor -= shift
    self.yymarker &-= shift  // May underflow if marker is unused
    self.token = 0

    // Fill free space at the end of buffer with new data from file.
    do {
      if let data = try self.file.read(upToCount: free) {
        self.buffer.replaceSubrange(self.yylimit..<(self.yylimit + data.count), with: data)
        self.yylimit += data.count
      }
    } catch {
      fatalError("cannot read from file: \(error.localizedDescription)")
    }
    self.buffer[self.yylimit] = 0  // append sentinel
    self.eof = self.yylimit < Self.bufferSize

    return .ok
  }

  enum FillStatus {
    case ok, eof, longLexeme
  }
}

@main struct Program {
  static func main() throws {
    let fileName = "input"
    let content = "'qu\0tes' 'are' 'fine: \\'' "

    // Prepare input file: a few times the size of the buffer,
    // containing strings with zeroes and escaped quotes.
    guard FileManager.default.createFile(
        atPath: fileName,
        contents: Data(String(repeating: content, count: Input.bufferSize).utf8)
    ) else {
      fatalError("failed to write file \"\(fileName)\"")
    }

    // Number of quoted strings written to file
    let count = 3 * Input.bufferSize

    // Initialize lexer state
    guard let file = FileHandle(forReadingAtPath: fileName) else {
      throw NSError(domain: NSCocoaErrorDomain, code: CocoaError.fileReadNoSuchFile.rawValue)
    }
    var `in` = Input(file: file)

    // Run the lexer
    assert(`in`.lex() == count)

    // Cleanup: remove input file
    try file.close()
    try FileManager.default.removeItem(atPath: fileName)
  }
}

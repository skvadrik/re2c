/* Generated by re2swift */
#sourceLocation(file: "05_fake_sentinel_eof_rule.re", line: 1)
// re2swift $INPUT -o $OUTPUT
import Foundation

func lex(_ str: Data) -> Int? {
  let length = str.count

  var cursor = 0, limit = length, marker = 0
  var count = 0

  loop: while true {
    
#sourceLocation(file: "05_fake_sentinel_eof_rule.swift", line: 15)
  var yych: UInt8 = 0
  var yystate: UInt = 0
  while true {
    switch yystate {
      case 0:
        yych = cursor < limit ? str[cursor] : 0
        switch yych {
          case 0x20:
            cursor += 1
            yystate = 3
            continue
          case 0x27:
            cursor += 1
            yystate = 5
            continue
          default:
            if limit <= cursor {
              yystate = 10
              continue
            }
            cursor += 1
            yystate = 1
            continue
        }
      case 1:
        yystate = 2
        continue
      case 2:
#sourceLocation(file: "05_fake_sentinel_eof_rule.re", line: 28)
        return nil
#sourceLocation(file: "05_fake_sentinel_eof_rule.swift", line: 46)
      case 3:
        yych = cursor < limit ? str[cursor] : 0
        switch yych {
          case 0x20:
            cursor += 1
            yystate = 3
            continue
          default:
            yystate = 4
            continue
        }
      case 4:
#sourceLocation(file: "05_fake_sentinel_eof_rule.re", line: 26)
        continue loop
#sourceLocation(file: "05_fake_sentinel_eof_rule.swift", line: 61)
      case 5:
        marker = cursor
        yych = cursor < limit ? str[cursor] : 0
        if yych >= 0x01 {
          yystate = 7
          continue
        }
        if limit <= cursor {
          yystate = 2
          continue
        }
        cursor += 1
        yystate = 6
        continue
      case 6:
        yych = cursor < limit ? str[cursor] : 0
        yystate = 7
        continue
      case 7:
        switch yych {
          case 0x27:
            cursor += 1
            yystate = 8
            continue
          case 0x5C:
            cursor += 1
            yystate = 9
            continue
          default:
            if limit <= cursor {
              yystate = 11
              continue
            }
            cursor += 1
            yystate = 6
            continue
        }
      case 8:
#sourceLocation(file: "05_fake_sentinel_eof_rule.re", line: 22)
        
        count += 1
        continue loop

#sourceLocation(file: "05_fake_sentinel_eof_rule.swift", line: 105)
      case 9:
        yych = cursor < limit ? str[cursor] : 0
        if yych <= 0x00 {
          if limit <= cursor {
            yystate = 11
            continue
          }
          cursor += 1
          yystate = 6
          continue
        }
        cursor += 1
        yystate = 6
        continue
      case 10:
#sourceLocation(file: "05_fake_sentinel_eof_rule.re", line: 27)
        return count
#sourceLocation(file: "05_fake_sentinel_eof_rule.swift", line: 123)
      case 11:
        cursor = marker
        yystate = 2
        continue
      default: fatalError("internal lexer error")
    }
  }
#sourceLocation(file: "05_fake_sentinel_eof_rule.re", line: 29)

  }
}

@main struct Program {
  static func main() {
    let test = { (str: String, expect: Int?) in
      // For the sake of example create a string without terminating null.
      assert(lex(Data(str.utf8)) == expect)
    }
    test("", 0)
    test("'qu\0tes' 'are' 'fine: \\'' ", 3)
    test("'unterminated\\'", nil)
  }
}

-- Generated by re2c
#1 "haskell/fill/02_fill.re"
-- re2hs $INPUT -o $OUTPUT
{-# OPTIONS_GHC -Wno-unused-record-wildcards #-}
{-# LANGUAGE OverloadedStrings #-}
{-# LANGUAGE RecordWildCards #-}

import Control.Monad
import Data.ByteString as BS
import GHC.IO.Handle
import System.Directory
import System.IO

chunk_size :: Int
chunk_size = 4096

data State = State {
    _file :: !Handle,
    _buf :: !BS.ByteString,
    _cur :: !Int,
    _lim :: !Int,
    _tok :: !Int,
    _eof :: !Bool
} deriving (Show)


#28 "haskell/fill/02_fill.hs"
yy0 :: State -> Int -> IO Int
yy0 !State{..} !_count = do
    State{..} <- fill State{..} 1
    yych <- return $ BS.index _buf _cur
    _cur <- return $ _cur + 1
    case yych of
        _c | 0x00 == _c -> do
            yy1 State{..} _count
        _c | 0x20 == _c -> do
            yy3 State{..} _count
        _c | 0x27 == _c -> do
            yy5 State{..} _count
        _c | True -> do
            yy2 State{..} _count

yy1 :: State -> Int -> IO Int
yy1 !State{..} !_count = do
#39 "haskell/fill/02_fill.re"
    return $ if _cur == _lim - yymaxfill + 1 then _count else (-1)
#48 "haskell/fill/02_fill.hs"

yy2 :: State -> Int -> IO Int
yy2 !State{..} !_count = do
#38 "haskell/fill/02_fill.re"
    return (-1)
#54 "haskell/fill/02_fill.hs"

yy3 :: State -> Int -> IO Int
yy3 !State{..} !_count = do
    State{..} <- fill State{..} 1
    yych <- return $ BS.index _buf _cur
    case yych of
        _c | 0x20 == _c -> do
            _cur <- return $ _cur + 1
            yy3 State{..} _count
        _c | True -> do
            yy4 State{..} _count

yy4 :: State -> Int -> IO Int
yy4 !State{..} !_count = do
#41 "haskell/fill/02_fill.re"
    lexer State{_tok = _cur, ..} _count
#71 "haskell/fill/02_fill.hs"

yy5 :: State -> Int -> IO Int
yy5 !State{..} !_count = do
    State{..} <- fill State{..} 1
    yych <- return $ BS.index _buf _cur
    _cur <- return $ _cur + 1
    case yych of
        _c | 0x27 == _c -> do
            yy6 State{..} _count
        _c | 0x5C == _c -> do
            yy7 State{..} _count
        _c | True -> do
            yy5 State{..} _count

yy6 :: State -> Int -> IO Int
yy6 !State{..} !_count = do
#40 "haskell/fill/02_fill.re"
    lexer State{_tok = _cur, ..} (_count + 1)
#90 "haskell/fill/02_fill.hs"

yy7 :: State -> Int -> IO Int
yy7 !State{..} !_count = do
    State{..} <- fill State{..} 1
    _cur <- return $ _cur + 1
    yy5 State{..} _count

lexer :: State -> Int -> IO Int
lexer !State{..} !_count = do
    yy0 State{..} _count

#42 "haskell/fill/02_fill.re"


yymaxfill :: Int
#106 "haskell/fill/02_fill.hs"
yymaxfill = 1
#45 "haskell/fill/02_fill.re"


fill :: State -> Int -> IO State
fill !st@State{..} !need =
    if _lim - _cur >= need then
        return st
    else case _eof of
        True -> error "fill failed"
        False -> do
            -- Discard everything up to the current token, cut off terminating null,
            -- read new chunk from file and reappend terminating null at the end.
            chunk <- BS.hGet _file chunk_size
            let !eof = BS.length chunk < need -- end of file ?
            let !buf = BS.concat [
                    BS.drop _tok _buf,
                    chunk,
                    if eof then (BS.replicate yymaxfill 0) else BS.empty]
            return State {
                _buf = buf,
                _cur = _cur - _tok,
                _lim = BS.length buf,
                _tok = 0,
                _eof = eof,
                ..}

main :: IO ()
main = do
    let fname = "input"

    -- Prepare input file.
    BS.writeFile fname $ BS.concat ["'qu\0tes' 'are' 'fine: \\'' " | _ <- [1..chunk_size]]
    let expect = 3 * chunk_size -- the total number of strings in file

    -- Run lexer on the prepared file.
    fh <- openFile fname ReadMode
    let st = State {
        _file = fh,
        _buf = BS.empty,
        _cur = 0,
        _tok = 0,
        _lim = 0,
        _eof = False
    }
    result <- lexer st 0
    hClose fh

    -- Cleanup.
    removeFile fname

    -- Check result.
    when (result /= expect) $ error $ "expected " ++ show expect ++ ", got " ++ show result
    return ()

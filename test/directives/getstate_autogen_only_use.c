/* Generated by re2c */
// re2c $INPUT -o $OUTPUT -if
// This test is for autogenerated state switch without `getstate:re2c`
// and with `use:re2c` blocks only (no non-use block that have code).

// rules block


// use block (expect a local state switch for this block only)


	switch (YYGETSTATE()) {
	default:
		goto yy0;
	case 0:
		goto yyFillLabel0;
	}
yy0:
	YYSETSTATE(0);
	if (YYLIMIT <= YYCURSOR) YYFILL(1);
yyFillLabel0:
	yych = *YYCURSOR++;
	{ x }


// use block (expect a local state switch for this block only)


	switch (YYGETSTATE()) {
	default:
		goto yy5;
	case 1:
		goto yyFillLabel1;
	}
yy5:
	YYSETSTATE(1);
	if (YYLIMIT <= YYCURSOR) YYFILL(1);
yyFillLabel1:
	yych = *YYCURSOR++;
	{ x }

